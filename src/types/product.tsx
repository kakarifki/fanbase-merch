export interface Product {
    id: string;
    code: string;
    name: string;
    description: string;
    price: number;
    imageUrl?: string;
    fanbase?: string;
    createdAt: string;
    updatedAt: string;
  }